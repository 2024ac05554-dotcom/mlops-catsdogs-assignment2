apiVersion: v1
kind: Service
metadata:
  name: catsdogs-inference-svc
  labels:
    app: catsdogs-inference
spec:
  type: NodePort
  selector:
    app: catsdogs-inference
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080
      protocol: TCP
